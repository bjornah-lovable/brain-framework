#!/usr/bin/env bash
# brain — SessionStart helper.
#
# Filesystem-only (MCP servers may not be connected yet on this hook).
# Currently a no-op placeholder; reserved for future use (e.g. writing
# a per-session index.txt of relevant pages).
#
# Always exit 0 — never block session start because of brain.

exit 0
